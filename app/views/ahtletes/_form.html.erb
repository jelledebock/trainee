<%= form_for(@ahtlete) do |f| %>
  <% if @ahtlete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ahtlete.errors.count, "error") %> prohibited this ahtlete from being saved:</h2>

      <ul>
      <% @ahtlete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
